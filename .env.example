# Claude Code Notification Configuration Example File | Claude Code 通知配置示例文件
# 
# Usage Instructions | 使用说明：
# 1. Copy this file as .env (already ignored in .gitignore) | 复制此文件为 .env（已在 .gitignore 中忽略）
# 2. Fill in your actual configuration values | 填入您的实际配置值
# 3. Add the following content to ~/.zshrc or ~/.bashrc | 在 ~/.zshrc 或 ~/.bashrc 中添加以下内容：
#    export BARK_KEY="your_actual_bark_key"
#    export BARK_SERVER="https://api.day.app"  # Optional, default value | 可选，默认值
#    export CLAUDE_NOTIFY_METHOD="bark"         # Optional: bark/terminal/both | 可选：bark/terminal/both

# Bark Notification Configuration | Bark 通知配置
# How to get: Install Bark App on your phone and get your unique key | 获取方法：在手机上安装 Bark App，获取您的专属 key
BARK_KEY="your_bark_key_here"

# Bark Server Address (optional, use default value) | Bark 服务器地址（可选，使用默认值即可）
# If you use self-hosted Bark server, modify this value | 如果您使用自建 Bark 服务器，请修改此值
BARK_SERVER="https://api.day.app"

# Notification Method Configuration | 通知方式配置
# bark: Use Bark mobile notification only (default) | 只使用 Bark 手机通知（默认）
# terminal: Use local terminal-notifier only | 只使用本地 terminal-notifier
# both: Use both notification methods | 同时使用两种通知方式
CLAUDE_NOTIFY_METHOD="bark"

# Important Notes | 注意事项：
# - Do not commit files containing real BARK_KEY to Git | 请勿将包含真实 BARK_KEY 的文件提交到 Git
# - Recommend adding environment variables to shell config file rather than storing in project directory | 建议将环境变量添加到 shell 配置文件中，而不是存储在项目目录
# - If BARK_KEY is not set, script will automatically fallback to terminal-notifier | 如果 BARK_KEY 未设置，脚本会自动回退到 terminal-notifier