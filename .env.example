# Claude Code 通知配置示例文件
# 
# 使用说明：
# 1. 复制此文件为 .env（已在 .gitignore 中忽略）
# 2. 填入您的实际配置值
# 3. 在 ~/.zshrc 或 ~/.bashrc 中添加以下内容：
#    export BARK_KEY="your_actual_bark_key"
#    export BARK_SERVER="https://api.day.app"  # 可选，默认值
#    export CLAUDE_NOTIFY_METHOD="bark"         # 可选：bark/terminal/both

# Bark 通知配置
# 获取方法：在手机上安装 Bark App，获取您的专属 key
BARK_KEY="your_bark_key_here"

# Bark 服务器地址（可选，使用默认值即可）
# 如果您使用自建 Bark 服务器，请修改此值
BARK_SERVER="https://api.day.app"

# 通知方式配置
# bark: 只使用 Bark 手机通知（默认）
# terminal: 只使用本地 terminal-notifier
# both: 同时使用两种通知方式
CLAUDE_NOTIFY_METHOD="bark"

# 注意事项：
# - 请勿将包含真实 BARK_KEY 的文件提交到 Git
# - 建议将环境变量添加到 shell 配置文件中，而不是存储在项目目录
# - 如果 BARK_KEY 未设置，脚本会自动回退到 terminal-notifier